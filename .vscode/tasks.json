{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "[Linux] ic11c: Compile current file",
            "type": "shell",
            "command": "${workspaceFolder}/compile.sh",
            "args": ["\"${file}\""],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
